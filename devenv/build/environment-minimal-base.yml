# Version matching according to PEP-440
# run: conda env create --file environment.yml
name: base
channels:
  - conda-forge
  - defaults
dependencies:
  - pdbpp
  - requests
  - neovim
  - isort
  - ipython
  - ipdb
